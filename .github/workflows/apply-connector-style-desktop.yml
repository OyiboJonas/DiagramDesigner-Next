name: Apply connector style desktop

on:
  push:
    branches: [editor-connector-style]
    paths:
      - scripts/apply-connector-style-desktop.py
      - .github/workflows/apply-connector-style-desktop.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: editor-connector-style
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0
          components: rustfmt, clippy
      - name: Apply desktop connector style changes
        run: python scripts/apply-connector-style-desktop.py
      - name: Format desktop Rust
        run: cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml
      - name: Desktop command ACL parity
        run: python scripts/check-desktop-command-acl.py
      - name: Desktop JavaScript syntax
        run: |
          node --check apps/desktop/ui/app.js
          node --check apps/desktop/ui/editor-interaction/connector-style-actions.mjs
      - name: Connector style frontend tests
        run: node --test web/editor-interaction/connector-style-actions.test.mjs
      - name: Full frontend interaction tests
        run: node --test web/editor-interaction/*.test.mjs
      - name: Workspace tests
        run: cargo test --locked --workspace
      - name: Workspace Clippy
        run: cargo clippy --locked --workspace --all-targets -- -D warnings
      - name: Diff check
        run: git diff --check
      - name: Commit product changes and remove temporary helpers
        shell: bash
        run: |
          set -euo pipefail
          git rm .github/workflows/apply-connector-style-desktop.yml scripts/apply-connector-style-desktop.py
          git add \
            apps/desktop/src-tauri/build.rs \
            apps/desktop/src-tauri/capabilities/main-editor.json \
            apps/desktop/src-tauri/permissions/editor.toml \
            apps/desktop/src-tauri/src/lib.rs \
            apps/desktop/ui/app.js \
            apps/desktop/ui/index.html \
            apps/desktop/ui/editor-interaction/connector-style-actions.mjs \
            web/editor-interaction/connector-style-actions.mjs \
            web/editor-interaction/connector-style-actions.test.mjs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Desktop: expose connector marker and line style controls"
          git push origin HEAD:editor-connector-style
